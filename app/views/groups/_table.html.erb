<% headings_partial ||= "headings" %>
<% row_partial ||= "row" %>

  <table>
    <%= render headings_partial %>
    <% @groups.each do |group| %>
      <%= render row_partial, group: group %>
    <% end %>
  </table>
